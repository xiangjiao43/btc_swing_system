# Sprint 2.5-A meta — CC 输出协议入库

**Date:** 2026-04-25
**Branch:** main
**Type:** docs / process

## 改动文件

| 文件 | +/− | 说明 |
|---|---|---|
| `CLAUDE.md` | +43 / −1 | 替换"协作规则 #1"指向新协议;新增 `## CC 输出协议` 完整章节 |

## 关键 diff

```diff
@@ ## 与用户协作规则 #1 @@
-1. 汇报格式:每个 Sprint 结束按简短三段(结果/自主决策/待关注),不要长篇大论
+1. 汇报格式:严格按下方「CC 输出协议」执行(每次任务完成后必读)
```

新增整段 `## CC 输出协议` 包含:
- 任务 1:写完整交付报告到 `docs/cc_reports/sprint_<编号>.md`
- 任务 2:对话里 4 段格式(一句话结果 / 决策点 / 风险 / 报告路径)
- 6 条禁止行为(贴 diff、贴命令输出、展示 TODO、主动跑 Playwright、提交临时产物、长段说明)
- 3 条允许行为(报告里详细写、引用 hash/文件名/行号、关键决策点必须停下问)

## 与现有"协作规则"的协调

旧"协作规则 #1"原文是"简短三段(结果/自主决策/待关注)" — 与新协议精神一致但
没有强制写报告文件这条。改成指针后,旧规则 #2-#5(Triggers / 即时 commit /
.env 保护 / 架构级歧义停下问)仍然有效,与新协议互补不冲突。

## 验收

- `git diff CLAUDE.md` 已展示给用户
- commit 单文件 docs change,不混代码改动
- push 后下次 CC 启动会自动加载新 CLAUDE.md

## 风险 / 未覆盖

- 协议本身只是"规则文本",真正能否执行要靠每次会话的 CC 自律
- "禁止主动跑 Playwright" 与之前 Sprint 2.3/2.5 流程冲突(以前是默认要跑的);
  下次前端任务时若用户没明说"截图给我看",CC 应该不主动截
- "禁止把截图提交 git" 与 sprint_2_5a_shots/ 等已存在的目录方向相反;
  历史目录保留不动(已 commit),从这次起遵守
